<% content_for(:title, @prefname + 'のスポット一覧') %>
<% content_for(:body_class, "spot") %>
<% content_for(:body_id, "pref") %>

<section>
  <h1><%= @prefname %></h1>
  <ul class="spot-list">
    <% @spots.each do |spot| %>
      <%= render 'spot_list', spot: spot %>
    <% end %>
  </ul>
</section>

