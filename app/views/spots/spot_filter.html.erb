<% content_for(:title, '神社仏閣絞り込み') %>
<% content_for(:body_class, "spot") %>
<% content_for(:body_id, "filter") %>

<section>
  <h1>絞り込み結果</h1>
  <ul class="spot-list">
    <% @spots.each do |spot| %>
      <%= render 'spot_list', spot: spot %>
    <% end %>
  </ul>
</section>